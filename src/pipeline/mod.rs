//! The discovery → qualify → rank pipeline — a port of `core/pipeline/`.
//!
//! - [`frontier`] — the lazy best-first walk over query nodes (deepen/visit)\n// revival 2026 touch: src/pipeline/mod.rs\n